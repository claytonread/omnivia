{
  "name": "omnivia-core",
  "version": "0.1.0",
  "private": true,
  "description": "Public OmniVia core primitives for local-first knowledge modeling, storage, graph links, import/export, and basic search.",
  "scripts": {
    "test": "echo \"Core tests are provided by the Python packages; run package-specific checks.\"",
    "lint": "echo \"Core linting is package-specific.\""
  }
}
